ranger
clear
ranger
clear
vim
la
sudo vim /etc/hosts
cd /mnt
sudo mkdir -p Data Video Audio Images Exe
sudo mkdir -p www
cd ~
ll
sudo vim /etc/fstab
sudo mount -a
sh -c "$(curl -fsSL https://raw.githubusercontent.com/robbyrussell/oh-my-zsh/master/tools/install.sh)"
sh -c "$(curl -fsSL https://raw.githubusercontent.com/robbyrussell/oh-my-zsh/master/tools/install.sh)"
alias
source .zshrc
cls
echo $SHELL
which zsh
